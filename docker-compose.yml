version: "3.8"

services:
  app:
    build: .
    container_name: aif-task-production
    restart: unless-stopped
    ports:
      - "5173:4173"
    environment:
      - NODE_ENV=production
    # Removed env_file since .env doesn't exist
    # Add back if .env file is created later:
    # env_file:
    #   - .env
    command: npm run preview